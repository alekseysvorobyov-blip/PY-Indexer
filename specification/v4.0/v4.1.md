# 1. ТЗ на разработку парсера исходников для ИИ

## Цель
Создать парсер Python проектов, который генерирует полную структурную карту проекта в формате TECH-INDEX v4.3, TECH-LOCATION v4.0, TECH-DOCSTRINGS v4.0, TECH-COMMENTS v4.0 и TECH-MAP v1.2.

## Требования к парсеру

### Входные данные
- Путь к корневой директории Python проекта
- Опциональные параметры:
  - Исключаемые директории (по умолчанию: `__pycache__`, `.git`, `venv`, `env`)
  - Максимальная глубина рекурсии
  - Формат вывода (JSON/minified JSON)

### Выходные данные
Файлы в указанной директории:
- `tech-index-{catalog}.json`
- `tech-location-{catalog}.json` 
- `tech-docstrings-{catalog}.json`
- `tech-comments-{catalog}.json`
- `tech-map-{catalog}.json`
- `tech-index-{catalog}-mini.json`
- `tech-location-{catalog}-mini.json` 
- `tech-docstrings-{catalog}-mini.json`
- `tech-comments-{catalog}-mini.json`
- `tech-map-{catalog}-mini.json`


### Функциональные требования

#### Этап 1: Подготовка и обход файлов
1. Рекурсивно найти все `.py` файлы в проекте
2. Исключить указанные директории
3. Для каждого файла сохранить относительный путь от корня проекта

#### Этап 2: Парсинг AST и извлечение сущностей
Для каждого Python файла:
1. Парсить AST дерево
2. Извлекать следующие сущности:
   - **Модули** (каждый файл = модуль)
   - **Классы** (включая вложенные)
   - **Функции/методы** (включая вложенные)
   - **Импорты** (import, from...import)
   - **Атрибуты** класса
   - **Переменные** уровня модуля

3. Для каждой сущности извлекать:
   - Имя
   - Родительскую сущность
   - Диапазон строк (start, end)
   - Докстринги
   - Комментарии
   - Декораторы
   - Типы параметров и возвращаемых значений
   - Значения по умолчанию параметров

#### Этап 3: Построение графов зависимостей
1. **Граф вызовов (call_graph)**:
   - Анализировать вызовы функций внутри тел функций
   - Связывать caller → callee через location_id
   - Учитывать методы классов и атрибуты

2. **Наследование (inheritance)**:
   - Определять MRO для каждого класса
   - Строить цепочки наследования через location_id

3. **Импорты (imports)**:
   - Различать внутренние и внешние импорты
   - Строить инвертированный индекс: модуль → файлы

#### Этап 4: Генерация TECH-LOCATION v4.0
1. Создать единое пространство location_id для всех сущностей
2. Для каждой сущности записать:
   - location_id (уникальный)
   - parent_location_id 
   - file_idx (индекс файла)
   - line_start, line_end
   - entity_type (0-7)

3. Построить hierarchy для быстрого поиска

#### Этап 5: Генерация TECH-INDEX v4.3
1. Создать пулы:
   - `names` - все уникальные имена
   - `type_strings` - аннотации типов
   - `default_values` - значения по умолчанию

2. Записать основные сущности:
   - modules: [name_idx, parent_location_id, location_id]
   - classes: [name_idx, parent_location_id, location_id] 
   - functions: [name_idx, parent_location_id, location_id]

3. Записать расширенную информацию:
   - `signatures` - параметры и возвращаемые типы
   - `class_info` - наследование и методы
   - `call_graph` - граф вызовов
   - `imports` - инвертированные импорты
   - `external_dependencies` - внешние модули

#### Этап 6: Генерация TECH-DOCSTRINGS и TECH-COMMENTS
1. Извлечь все докстринги и комментарии
2. Создать пулы текстов
3. Связать с location_id сущностей

#### Этап 7: Генерация TECH-MAP v1.2
1. Построить быстрые индексы:
   - `file_entities` - сущности по файлам
   - `name_entities` - сущности по именам  
   - `type_entities` - сущности по типам

2. Добавить ссылки на основные файлы

### Нефункциональные требования
- **Производительность**: Обработка 1000 файлов за < 30 секунд
- **Память**: Использование < 500MB RAM для крупных проектов
- **Надежность**: Устойчивость к синтаксическим ошибкам в коде
- **Полнота**: Извлечение 95%+ структурной информации

### Алгоритм работы
```
1. Обход файловой системы
   ↓
2. Парсинг AST каждого файла
   ↓  
3. Извлечение сущностей и location_id
   ↓
4. Построение графов зависимостей
   ↓
5. Генерация TECH-LOCATION
   ↓
6. Генерация TECH-INDEX  
   ↓
7. Генерация TECH-DOCSTRINGS/COMMENTS
   ↓
8. Генерация TECH-MAP
   ↓
9. Сериализация в MessagePack
```

# 2. Описание схем на человеческом языке

## Общая концепция

Мы создаем **5 связанных файлов**, которые вместе дают полную карту Python проекта:

### 🗺️ **TECH-LOCATION v4.0** - "Географическая карта"
**Что хранит:** Координаты ВСЕХ сущностей в коде
**Как выглядит:** 
```
"locations": [
  [0, -1, 0, 1, 312, 0],    # файл: id=0, родитель=-1, файл=0, строки 1-312
  [1, 0, 0, 1, 9, 5],       # докстринг: id=1, родитель=0 (файл), строки 1-9
  [2, 0, 0, 34, 154, 2],    # класс: id=2, родитель=0, строки 34-154
  [3, 2, 0, 40, 52, 5],     # докстринг класса: id=3, родитель=2 (класс)
]
```
**Зачем нужно:** ИИ знает ГДЕ что находится и какие границы у каждой сущности

### 🏗️ **TECH-INDEX v4.3** - "Архитектурный чертеж" 
**Что хранит:** Структурные связи и зависимости
**Основные разделы:**
- `names` - словарь всех имен в проекте
- `modules/classes/functions` - кто есть кто и кто кому родитель
- `signatures` - какие параметры принимают функции
- `class_info` - от кого наследуются классы и какие методы имеют
- `call_graph` - кто кого вызывает
- `imports` - что откуда импортируется

**Зачем нужно:** ИИ понимает КАК устроен проект и какие зависимости между компонентами

### 📝 **TECH-DOCSTRINGS v4.0** - "Документация"
**Что хранит:** Все строки документации
**Как устроено:**
```
"docstrings_text": ["Описание функции...", "Описание класса..."],
"docstrings": [[1, 0], [3, 1]]  # location_id → индекс текста
```
**Зачем нужно:** ИИ понимает ЧТО делают функции и классы

### 💬 **TECH-COMMENTS v4.0** - "Примечания на полях"
**Что хранит:** Все комментарии в коде
**Как устроено:** Аналогично докстрингам
**Зачем нужно:** ИИ понимает контекст и особенности реализации

### 🗂️ **TECH-MAP v1.2** - "Указатель"
**Что хранит:** Быстрые индексы для навигации
**Основные индексы:**
- `file_entities` - что находится в каждом файле
- `name_entities` - где используется каждое имя  
- `type_entities` - все сущности по типам

**Зачем нужно:** Быстрый поиск без загрузки полных данных

## Как это работает вместе

### Пример: ИИ хочет изменить метод `run` в `cli_index.py`

1. **Быстрый поиск** (TECH-MAP):
   - Находит `run` → location_id = 5
   - Находит файл `cli_index.py` → entities = [1, 2, 5, 6...]

2. **Понимание контекста** (TECH-LOCATION):
   - location_id=5: метод `run`, родитель=2 (класс IndexCLI)
   - Границы: строки 58-122
   - Нельзя выходить за эти границы!

3. **Анализ зависимостей** (TECH-INDEX):
   - `call_graph`: run вызывает [16, 17, 18] (get_logger, configure_root_logger...)
   - `signatures`: параметры `args: list[str]`, возврат `int`
   - `class_info`: IndexCLI не наследуется, методы = [5, 6, 7]

4. **Понимание назначения** (TECH-DOCSTRINGS):
   - Читает докстринг метода run
   - Понимает что делает функция

5. **Безопасное изменение:**
   - Не сломает границы класса
   - Не нарушит вызовы зависимостей  
   - Сохранит сигнатуру
   - Обновит документацию если нужно

## Ключевые принципы

### 🎯 **Location-based архитектура**
Все связи через `location_id`, а не имена. Это защищает от:
- Дублирования имен в разных модулях
- Переименований
- Динамического кода

### 📦 **Пул-оптимизация**
Повторяющиеся строки (имена, типы, значения) хранятся в пулах. Экономит 50-70% размера.

### 🔗 **Иерархичность** 
Каждая сущность знает своего родителя. ИИ понимает вложенность и границы.

### 🚀 **Разделение данных**
- TECH-LOCATION: ГДЕ находится
- TECH-INDEX: КАК связано  
- TECH-MAP: БЫСТРЫЙ поиск
- TECH-DOCSTRINGS/COMMENTS: ЧТО делает

Это позволяет ИИ загружать только нужные данные для конкретной задачи.

Такой подход дает ИИ полное понимание проекта для безопасного и точного рефакторинга.